#ifndef CARGARDATOS_H_INCLUDED
#define CARGARDATOS_H_INCLUDED

#include "Lista.h"

Lista* cargarMaterias();
Lista* cargarEstudiantes(Lista*);
Lista* cargarMateriasSistema(Lista*, Lista*);

#endif // CARGARDATOS_H_INCLUDED
